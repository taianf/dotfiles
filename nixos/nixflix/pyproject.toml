[project]
name = "nixflix"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["typer"]

[tool.ruff.lint]
select = ["I", "F", "W", "E", "PL", "RUF"]

[tool.ruff.lint.isort]
force-single-line = true

[tool.uv]
package = true
